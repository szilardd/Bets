@model GoalscorerModel
@{ Layout = null; }
@if (ViewBag.Expired)
{
    if (ViewBag.Count > 0)
    {
        <div id="goalscorer-for-round-wrapper">
            <div>
                <h1 style="text-align: center;">Goalscorer for @ViewBag.RoundName</h1>
            </div>
            <div>
                <img class="icon full" src="@Url.GetPlayerImage(Model.ExternalID)" />
            </div>
            <div class="goalscorer-name">
				<img class="flag" src="@Url.GetTeamFlagImage(Model.TeamFlag)" /><
                <b>@Model.Name</b>
            </div>
            <div><b>@Model.Points</b> points</div>
            <div><b>@Model.GoalsScored</b> goal@(Model.GoalsScored != 1 ? "s" : "")</div>
        </div>
    }
    else
    {
        <br />
        <h1 class="center-text">No selected Top Goalscorer for @ViewBag.RoundName yet.</h1>
        <br />
    }
}
else
{
    <br />
    <h1 class="center-text">You are not allowed to see the others Top Goalscorer bets for @ViewBag.RoundName yet.
    </h1>
    <br />
}